---
name: Colors
menu: Styles
route: '/styles/colors'
---


import { colors, accentColors, uiColors, legacyColors } from './';
import ColorBox from '../docs/components/ColorBox'

# Colors 

### Legacy 
<div>
{Object.entries(legacyColors).map(([name, hex]) => (
    <ColorBox name={name} hex={hex} />
))}
</div>

### UI
<div>
{Object.entries(uiColors).map(([name, hex]) => (
    <ColorBox name={name} hex={hex} />
))}
</div>

### Accent
<div>
{Object.entries(accentColors).map(([name, hex]) => (
    <ColorBox name={name} hex={hex} />
))}
</div>
