---
name: Colors
menu: Styles
route: '/styles/colors'
---


import colors from './colors';
import ColorBox from '../docs/components/ColorBox'
import toPairs from 'lodash/toPairs'

# Colors 

<div>
{toPairs(colors).map(([name, hex]) => (
    <ColorBox name={name} hex={hex} />
))}
</div>
