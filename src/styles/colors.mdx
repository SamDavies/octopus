---
name: Colors
menu: Styles
route: '/styles/colors'
---


import { colors, accentColors, uiColors, primaryColors } from './';
import ColorBox from '../docs/components/ColorBox'
import toPairs from 'lodash/toPairs'

# Colors 

### Primary 
<div>
{toPairs(primaryColors).map(([name, hex]) => (
    <ColorBox name={name} hex={hex} />
))}
</div>

### UI
<div>
{toPairs(uiColors).map(([name, hex]) => (
    <ColorBox name={name} hex={hex} />
))}
</div>

### Accent
<div>
{toPairs(accentColors).map(([name, hex]) => (
    <ColorBox name={name} hex={hex} />
))}
</div>
