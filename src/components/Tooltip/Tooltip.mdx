---
name: Tooltip
menu: Components
route: '/components/tooltip'
---


import { Fragment } from 'react'
import Tooltip, { StyledTooltip } from './'
import { Playground, Props } from 'docz'
import Button from '../../docs/components/Button'
import Image from '../../docs/components/Image'

# Tooltip

This is a wrapper that adds a tooltip to a child element.
 
## Properties
<Props of={Tooltip} />

## Basic usage

<Playground>
  <ul>
    <li>
        <Tooltip
            renderTrigger={'some normal text with a tooltip'}
            renderContent={'and some normal text in the tooltip'}
            position='top'
        />
    </li>
    <li>
        <Tooltip
            renderTrigger={<Button text="a demo tooltip with more complex content and trigger" />}
            renderContent={<Image src='https://lesbaa.github.io/img/z-loki.gif' text='an image!' />}
            position='right'
        />
    </li>
    <li>
        <Tooltip
            renderTrigger={<Button text="a demo tooltip with a function render prop" />}
            renderContent={() => <Image src='https://lesbaa.github.io/img/z-loki.gif' text='an image but in a functional render prop!' />}
            position='right'
        />
    </li>
    <li>
        <Tooltip
            renderTrigger={<Button text="a demo tooltip with the tooltip disabled" />}
            renderContent={() => <Image src='https://lesbaa.github.io/img/z-loki.gif' text='an image but in a functional render prop!' />}
            position='right'
            disableTooltip
        />
    </li>
  </ul>
</Playground>