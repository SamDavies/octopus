---
name: Checkbox
menu: Components
route: '/components/Checkbox'
---



import Checkbox from './'
import { useState, Fragment } from 'react'
import BlockContainer from '../../docs/components/BlockContainer'
import { Playground, Props } from 'docz'

# Checkbox

Checkbox component. Handles both radio and checkbox inputs. There are hover states and focus states to indicate being active. And the component will also handle keyboard events.
 
## Properties
<Props of={Checkbox} />

## Basic usage

### Primary style
<Playground>
{() => {
    const [isChecked, setIsChecked] = useState(false)
    return (
        <Fragment>
         <BlockContainer>
            <Checkbox
                id='check1'
                name='check1'
                value='check1'
                onChange={() => setIsChecked(!isChecked)}
                checked={isChecked}
                labelText="toggleable checkbox"
            />
        </BlockContainer>
         <BlockContainer>
            <Checkbox
                id='check2'
                name='check2'
                value='check2'
                onChange={() => {}}
                checked={true}
                labelText="Checkbox with check"
            />
        </BlockContainer>
         <BlockContainer>
            <Checkbox
                id='check3'
                name='check3'
                value='check3'
                onChange={() => {}}
                checked={false}
                labelText="Checkbox without check"
            />
        </BlockContainer>
        
         <BlockContainer>
            <Checkbox
                id='check3'
                name='check3'
                value='check3'
                onChange={() => {}}
                checked={false}
                disabled={true}
                labelText="Disabled checkbox"
            />
        </BlockContainer>
        
        </Fragment>
    )    
}}
</Playground>